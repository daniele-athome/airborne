{
  "name": "airborne-server",
  "version": "0.0.0",
  "private": true,
  "description": "Apps Script backend for Airborne: atomic writes to the Google Sheets stores",
  "scripts": {
    "test": "node --test 'test/*.test.js'",
    "push": "clasp push",
    "deploy": "./deploy.sh"
  },
  "devDependencies": {
    "@google/clasp": "^3.4.0",
    "@types/google-apps-script": "^2.0.12"
  }
}
