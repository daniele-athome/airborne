#!/bin/bash
# Called as postchangelog script by standard-version.
# Used to copy the contents of the latest version in Fastlane what's new metadata files.

# TODO extract changes from latest version (which ones?)
